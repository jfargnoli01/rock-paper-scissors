class Player {
  constructor(name) {
    this.name = name;
    this.token = name === 'Computer' ? '🖥' : '🤓';
    this.wins = 0;
  };

  saveWinsToStoarge() {

  };

  retrieveWinsFromStorage() {

  };

  takeTurn() {

  };
};